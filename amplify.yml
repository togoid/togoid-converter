version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 24
        - node -v
        - yarn
    build:
      commands:
        - yarn build2
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*